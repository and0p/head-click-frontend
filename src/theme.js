import { createMuiTheme } from 'material-ui/styles';

const theme = createMuiTheme({
    palette: {
      primary: {
        main: '#8941af'
      },
      secondary: {
        main: '#4878c0'
      },
      background: {
        default: "#1e1d23",
        paper: "#25252f"
      },
      type: 'dark'
    },
  });

export default theme;